## 0.1.0

- Initial release: cimbar decoder app — Web (WASM) and Android (FFI).
- Viewfinder per the official receivers (recv.html / cfc), Auto mode
  rotation with lock-on-detect, official recv-worker.js decode pool.
