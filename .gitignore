# ====================
# OS
# ====================
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ====================
# IDE / Editor
# ====================
# VS Code
.vscode/
*.code-workspace

# IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws

# Visual Studio (C++ native builds)
*.suo
*.user
*.userosscache
*.sln.docstates
*.sdf
*.opensdf
.vs/

# ====================
# Dart / Flutter
# ====================
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
coverage/
doc/api/

# Pub lock file — do NOT commit for libraries/plugins
pubspec.lock

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# FFI generated bindings (regenerated via ffigen)
lib/src/ffi/generated/

# ====================
# Native C++ build output
# ====================
native/build_windows/
native/build_android/
native/build_wasm/
*.obj
*.o
*.lib
*.dll
*.so
*.dylib
*.exp
*.pdb
*.ilk
*.vcxproj
*.vcxproj.filters
*.sln
*.vcxproj.user

# ====================
# Android
# ====================
android/local.properties
android/.gradle/
android/build/
android/src/main/cpp/build/
*.apk
*.aab
*.jks
*.keystore
android/src/main/cpp/.externalNativeBuild/
android/src/main/cpp/.cxx/
# AGP puts the CMake build cache at the MODULE root, not under src/main/cpp.
# The rule above misses it — which is how 93 build-cache files got committed.
android/.cxx/
android/.externalNativeBuild/

# ====================
# Web / WASM assets (built separately, not source)
# ====================
assets/wasm/*.wasm
assets/wasm/*.js
!assets/wasm/.gitkeep

# ====================
# WASM regression test scratch (test/wasm/run.sh compiles dump_frames here)
# ====================
test/wasm/.build/

# ====================
# Example app
# ====================
example/build/
example/.dart_tool/
example/.flutter-plugins
example/.flutter-plugins-dependencies
example/pubspec.lock
example/android/local.properties
example/android/.gradle/
example/android/app/build/
example/windows/flutter/ephemeral/
example/web/assets/wasm/

# ====================
# Misc
# ====================
*.log
*.class
*.pyc
*.swp
.history
.svn/

# Qoder / AI agent screenshots
*.png
!assets/**/*.png
!example/**/assets/**/*.png

# ====================
# Vendored libcimbar C++ core (third_party/libcimbar_cpp)
# ====================
# Track the full source tree, INCLUDING bitmap/*.png tiles (loaded at runtime
# by the C++ core) which the global *.png rule above would otherwise ignore.
# Build outputs are still ignored via third_party/libcimbar_cpp/.gitignore.
!third_party/libcimbar_cpp/**
# ...but keep local, machine-specific dev helpers out of git:
third_party/libcimbar_cpp/.clangd
third_party/libcimbar_cpp/compile_commands.json
third_party/libcimbar_cpp/.cache/
